<h2>Skills</h2>

<div class="row">
    <ul>
        {% for skills in page.skills|batch(page.skills|length / 2) %}
            <span class="col-md-6">
                {% for skill in skills %}
                    <li>
                        {{ skill.name }} - {{ page.skill_levels[skill.level] }}
                    </li>
                {% endfor %}
            </span>
        {% endfor %}
    </ul>
</div>

<hr>
